import { ComponentType } from "react";
import { Typography } from "@mui/material";

const Preferences: ComponentType<{}> = () => {
    return (
        <Typography>
            This is the preferences page
        </Typography>
    );
}

export default Preferences